<%= form_for(@room_feature) do |f| %>
  <% if @room_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room_feature.errors.count, "error") %> prohibited this room_feature from being saved:</h2>

      <ul>
      <% @room_feature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
