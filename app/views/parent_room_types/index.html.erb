<p id="notice"><%= notice %></p>

<h1>Listing Parent Room Types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @parent_room_types.each do |parent_room_type| %>
      <tr>
        <td><%= parent_room_type.name %></td>
        <td><%= link_to 'Show', parent_room_type %></td>
        <td><%= link_to 'Edit', edit_parent_room_type_path(parent_room_type) %></td>
        <td><%= link_to 'Destroy', parent_room_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Parent room type', new_parent_room_type_path %>
